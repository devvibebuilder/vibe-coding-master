{
  "name": "vibe-coding-master",
  "version": "1.0.0",
  "description": "Framework de gobernanza técnica para desarrollo asistido por IA (Clean Architecture + Zod)",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "audit:contracts": "ts-node scripts/audit-interfaces.ts",
    "test": "jest",
    "check:types": "tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "validate": "npm run check:types && npm run audit:contracts"
  },
  "keywords": [
    "vibe-coding",
    "clean-architecture",
    "ai-governance",
    "zod",
    "typescript"
  ],
  "author": "Gustavo Reyes",
  "license": "GPL-2.0",
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
